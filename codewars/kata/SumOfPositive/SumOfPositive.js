function positiveSum(arr) {
  return arr.reduce((sum, current) => sum + (current > 0 ? current : 0), 0);
}

/* Эта функция JavaScript positiveSumп ринимает массив arrв качестве входных данных.

Функция использует reduce метод массива arrдля перебора его элементов и сокращения его до одного значения, которое представляет собой сумму всех положительных чисел в массиве.

Метод reduce принимает два аргумента:

функция обратного вызова, которая указывает, как вычислить уменьшенное значение, и
начальное значение (в данном случае 0).
Функция обратного вызова принимает два параметра: sum и current.
sum— это накопленное значение, которое обновляется на каждой итерации, и currentэто текущий обрабатываемый элемент.

Функция обратного вызова использует тернарный оператор ? :, чтобы определить, является ли currentон положительным или нет. Если currentположительное, оно добавляется к sum, в противном случае добавляется 0.

Наконец, reduceметод возвращает окончательное уменьшенное значение, которое представляет собой сумму всех положительных чисел в arr, и positiveSum функция возвращает это значение в качестве результата. */
